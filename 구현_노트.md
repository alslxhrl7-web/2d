# 25라운드 2D 디펜스 — 실행/구현 노트

Unity 6000.6.0f1, 2D URP 템플릿, Input System 전용 프로젝트입니다.
씬에는 아무것도 배치돼 있지 않습니다 — `Assets/Scripts/Core/GameBootstrapper.cs`가 실행 시점에
카메라·배경·길·타워/웨이브/UI 매니저를 전부 코드로 조립합니다.

---

## 1. 실행 방법

### 유니티 에디터에서
1. Unity Hub에서 이 프로젝트(`2d`)를 엽니다.
2. 아무 씬이나 열어도 됩니다(`Assets/Scenes/SampleScene.unity` 그대로 사용).
3. Play(▶)를 누르면 바로 시작됩니다.

### 유니티 없이 독립 실행 파일로
1. 에디터 상단 메뉴에서 **`Defense2D > Windows 실행 파일 빌드`** 를 누릅니다.
2. 빌드가 끝나면 탐색기가 자동으로 열립니다. 결과물 위치:
   `<프로젝트 폴더>\Build\Windows\2d.exe`
3. 이 **`2d.exe`를 더블클릭하면 유니티 없이 바로 실행**됩니다.

> **다른 PC로 옮길 때**: `2d.exe` 하나만 복사하면 실행되지 않습니다. 같은 폴더의
> `2d_Data` 폴더, `UnityPlayer.dll` 등 **`Build\Windows` 폴더 전체를 통째로** 복사하세요.

> **창 모드로 뜹니다**: 빌드 스크립트가 1600×900 창 모드(크기 조절 가능)로 설정합니다.
> 게임 도중에는 종료 버튼이 없어서 전체화면이면 빠져나오기 어렵기 때문입니다. 창의 X 버튼으로
> 언제든 닫을 수 있고, 게임오버/승리 화면에는 "게임 종료" 버튼도 있습니다.
> 전체화면으로 바꾸려면 `Project Settings > Player > Resolution and Presentation`에서 조정하세요.

빌드 스크립트는 `Assets/Editor/BuildScript.cs`에 있고, 명령줄 빌드(`-executeMethod`)도
지원합니다. 자세한 사용법은 그 파일의 주석을 참고하세요.

---

## 2. 조작

| 입력 | 동작 |
|---|---|
| `TAB` | 건설 메뉴 열기/닫기 |
| 마우스 왼쪽 클릭 | 타워 배치 / 철거 (건설 메뉴에서 모드 선택 후) |
| 마우스 오른쪽 클릭, `ESC` | 배치·철거 모드 취소 |
| 우측 상단 버튼 | 준비 단계에선 "웨이브 시작", 방어 중에는 "웨이브 스킵"(10초 후 활성화) |

타워는 **설치하는 순간 타입이 무작위로 결정**됩니다. 배치 모드에서 고스트가 세 타입을 빠르게
돌아가며 보여주고, 클릭한 순간 보이던 타입 그대로 설치됩니다. 철거하면 건설비의 50%를
돌려받습니다.

---

## 3. 게임 구조

- **3스테이지 × 25웨이브** (총 75웨이브). 화면에는 `STAGE N · WAVE MM`으로 표시됩니다.
- **길**: 도착점 없는 정사각형 순환 루프. 적은 처치되기 전까지 영원히 맴돕니다.
  길은 한 스테이지 안에서 고정이고, 스테이지가 바뀔 때만 다른 도안으로 교체됩니다.
  마주보는 두 지점에서 동시에 적이 나옵니다(길 위에 체크무늬로 표시).
- **패배 조건**: 거점 개념이 없습니다. ① 동시 생존 적이 한도(시작 50마리)를 넘으면 즉시 패배,
  ② 스테이지 피날레(로컬 25웨이브) 보스를 60초 안에 못 잡으면 즉시 패배.
- **보스**: 로컬 웨이브 10, 20에 단독 등장. 로컬 25웨이브는 "피날레"로 보스와 일반 유닛 50마리가
  함께 몰아칩니다. 마지막 스테이지의 피날레는 항상 3페이즈 "최종 보스"입니다.
- **적 3종**: 잡몹 / 돌진형(고속) / 방패병(고체력, 타워 피해만 50% 경감).
- **타워 3종**: 화살탑 25골드(단일 대상 속사 + 약한 슬로우), 빙결탑 25골드(범위 슬로우),
  포격탑 50골드(광역 폭발). 타입별 피해가 실제로 적용되는 방식은 `Projectile.Hit()` 주석에
  한데 정리해 뒀습니다.
- **업그레이드 3종**: 웨이브 클리어마다 선택 — 수용력 강화 / 재화 감각 / 타워 강화.
- **배경**: 스테이지마다 숲 → 병든 숲(11~19) → 오염된 숲(20~24) → 보스전(25) 4단계로
  교차 페이드됩니다.

---

## 4. 코드 구조

```
Assets/
  Editor/BuildScript.cs        빌드 메뉴 (에디터 전용, 게임 빌드에는 포함되지 않음)
  Resources/Sprites/           타워·적·보스·배경 아트 (없으면 도형으로 자동 폴백)
  Scripts/
    Core/       GameBootstrapper(전체 조립·재시작), GameManager(상태머신·골드),
                GameConstants(밸런스 상수), StageBackground(배경 전환), Upgrades
    PathSystem/ PathData, PathLibrary(스테이지별 길 도안)
    Enemies/    EnemyController, BossController, EnemyType
    Towers/     TowerBase, ArrowTower, IceTower, CannonTower, Projectile, ImpactEffect
    Build/      BuildManager(배치·철거)
    Waves/      WaveManager(웨이브·스테이지·보스), WaveData
    UI/         UIManager(HUD 전체를 코드로 생성)
    Util/       SpriteFactory(도형 스프라이트 런타임 생성)
```

**"다시 시작" 동작 방식**: `RuntimeInitializeOnLoadMethod`는 게임 시작 시 딱 한 번만 불리므로,
씬만 다시 불러오면 조립 코드가 돌지 않아 빈 화면이 됩니다. 그래서 `GameBootstrapper`가
`SceneManager.sceneLoaded`에 구독해 두고, 씬이 다시 로드될 때마다 게임을 새로 조립하면서
씬을 넘어 살아남는 static 값(타워 공격력 배율 등)도 함께 초기화합니다.

---

## 5. 밸런스를 만질 때 보는 곳

| 조정하고 싶은 것 | 파일 / 상수 |
|---|---|
| 시작 골드, 타워 비용, 철거 환급률 | `GameConstants.StartingGold` / `TowerCost` / `TowerRefundPercent` |
| 준비 시간, 스킵 후 준비 시간 | `GameConstants.PrepPhaseSeconds` / `PrepPhaseSecondsAfterSkip` |
| 피날레 보스 제한시간 | `GameConstants.StageFinaleBossTimeLimit` |
| 동시 생존 한도 | `GameConstants.StartingMaxAliveEnemies` |
| 적 처치 보상, 보스 보상, 난이도 증가 곡선 | `WaveManager.SpawnEnemy()` |
| 웨이브 클리어 보너스 | `GameManager.OnWaveClearedHandler()` |
| 타워 사거리·연사·피해 | 각 타워의 `Setup()` |
| 웨이브 스킵 대기 시간 | `WaveManager.SkipUnlockSeconds` |
| 배경 밝기·색조 | `StageBackground`의 틴트 상수 |

세션별 변경 이력과 설계 의도는 Claude 프로젝트 문서 `claude/구현_기록.md`에 정리돼 있습니다.
